version: '3'

services:
  django:
    build:
      context: .
      dockerfile: containers/django/Dockerfile
    
    container_name: django

    ports:
      - "8000:8000"
    
    env_file:
      - .env

    tty: true

    volumes:
      - ${SRC_PATH}:/root/workspace   

